// main fungsi di eksejusi pertasma kali
// Main argumen(java) atau main parameter(kotlin) iyu opsional di kotlin

fun main(args: Array<String>) {
    for (data in args){
        println(data)
    }
    // data bisa di dapatkan dari program arguments, tepatnya di samping palu dan run atas kanan itu ada nama filenya di klik lalu edit konfigurasi lalu isi program argumen

    // misal di isi satu kata itu per array, kalau mau mengisi 1 kalimat per array tambahkan "", spasi untuk pemisah
}